#include "main.h"
/**
 * main - Entry point
 * Return: always 0 if success
 */
int main(void)
{
_puts("\"At the end of the day, my goal was to be the best hacker\"\t");
_puts("- Kevin Mitnick");
return (0);
}
